import item from '../models/item/item';

export const blueSquare = () =>{
    const blueSquare = new item(
        "Blue Square",
        "No idea what this actually does",
        0
    )
    return blueSquare;
}